// Top-level build file. Plugin versions are kept in step with the sibling
// full Android app (jev-chat-jarvis) so both build with the same toolchain.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
